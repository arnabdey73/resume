I'm applying for the Sr Cloud Engineer role on the Cloud Platform team. The shape of the work — Azure-primary, AKS-powered, Terraform from the ground up, multi-region, security-first, with on-call as a real engineering activity — lines up with what I've been doing for the past several years. Stockholm-based, remote-from-Sweden, existing right to work — no sponsorship required.

"Designed from scratch, not template tourism" is the easiest way to describe the recurring thread. At **Stena Metall** I authored two **custom Azure RBAC role definitions** — a "Backup Remover" role and a Contributor variant with explicit `notActions` — to enforce separation-of-duties between resource management and backup deletion (a ransomware-resilience control absent from Azure's built-in Contributor). Wired into a CAF Enterprise Scale archetype framework across ~100 subscriptions. At **Volvo Cars** I designed and owned a **GitOps self-service Azure Databricks platform** — engineers request compute via a GitHub issue, a YAML manifest PR is validated by GitHub Actions, merge applies the change; lead time fell from days to same-day, and idle-cluster spend was eliminated by construction. At **Cisco** I authored the Terraform for the **CD 2.0 platform on AWS** — VPC, EKS, IAM, ALB, ECR, Jenkins on EKS — alongside a separate Prometheus + Grafana observability stack.

Security-first is the second thread. **SAML 2.0 federation** at Channel 7 replaced long-lived IAM user keys with a real identity flow. **X-Pack security with SSL and snapshot repositories** at Volvo gave the production Elasticsearch cluster end-to-end TLS, role-based access, and tested backup/restore. A live K3s reference platform on GitHub runs **cert-manager + sealed-secrets** to keep cert lifecycle and secret material in declarative form. At Stena Metall I delivered a **tenant-wide Key Vault compliance audit** and a Service Principal credential-rotation design (Logic Apps + Microsoft Graph `addPassword`).

Multi-cloud and multi-region come up too. At Volvo I built **two matched Terraform stacks** — AWS S3 + KMS + IAM on one side, Azure Premium BlockBlobStorage on the other — as identically-shaped object-storage benchmarking rigs. At **Deutsche Telekom** I owned the platform side of a ~30-microservice 5G BSS commercial pilot on PAN-NET (OpenStack) and led a **single-weekend cross-tenant migration** that rehomed 54 Tuleap repositories, Jenkins jobs, Nexus artifacts, Keycloak realm, and live Kubernetes workloads — incident-grade coordination, nothing rebuilt from scratch. Earlier, three and a half years of **24×7 shift-based Linux operations** at HP Enterprise Services — storage, SAN, LVM, kernel tuning — is where the on-call reflexes come from.

One honest note: my scale is enterprise infrastructure (~100 Azure subscriptions, ~36-node bare-metal Kubernetes, ~30 microservices), not consumer SaaS at "millions of users." The operational rigour transfers; the consumer-scale patterns I'd treat as a ramp.

Happy to pick up the thread in a conversation.

Warm regards,
Arnab Dey
